[pytest]
testpaths = tests
addopts = -sv -m "not integration"
asyncio_mode = auto
markers =
    integration: marks tests as integration tests requiring real infrastructure (deselect with '-m "not integration"')
    unit: marks tests as unit tests (select with '-m unit')
    azure: marks tests as Azure-specific integration tests (select with '-m azure')